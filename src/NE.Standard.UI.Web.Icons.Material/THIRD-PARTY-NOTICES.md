# Third-party notices

This package embeds and redistributes a font file.

## Material Symbols

- **What:** `Client/dist/ui-icons-material.woff2` — Material Symbols Rounded, subset to its own glyph set and
  with the `wght` and `GRAD` axes pinned. Built by `Client/scripts/build-icons.mjs` from the
  [`material-symbols`](https://www.npmjs.com/package/material-symbols) package, which repackages Google's
  [material-design-icons](https://github.com/google/material-design-icons).
- **Copyright:** Google LLC.
- **Licence:** Apache License 2.0 — the full text is in `LICENSE-material-symbols.txt`, distributed with this
  package as clause 4 requires.

The glyph names in `NE.Standard.UI.Icons.Material.MaterialIcons` are generated from the same source and carry
the same licence.

The package's own code is MIT.
